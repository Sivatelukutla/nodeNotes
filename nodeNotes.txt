Note : 
    in node js has a two main components (1) v8 javascript engine (2)libuv 
Note : 
    in node js have a super power examples=>core modules and loat of thinks 
Note:
    the node js code or javascript code are syncronouslly executes 
Note:
    the syncronouslly code is store in callstack and asyncronous code is store in libuv 
Note:
    functions like function decelration and function expression are stroed in callstack, in callstack have two segements are there 
    (1)global execution context (2)execution context. the function are stored in executes context 
    
------------------------------event loop-----------------------------------------
Note: 
    =>=>in event loop have 4 major phases are there 
    *timer => setTimeout,setInterval 
    *poll  => i/o callbacks LIKE reding file, fs, http, get => all asyncronous code executes then its execution
    *check => set immediatelly 
    *close => socket . ("close")  
Note: 
    All phase are waiting in callback quee 
    IF => callstack is empty that phases of code is pushed into callstack 

Note:
    before going to the every phase in the event loop. in event loop have a  *process.next Tick(), *promise callback checks the those two point 

NOTE=>=>=>=>=>=>=>
    this is exmaple of event loop phase 
    const a = 100;

    setImmediate(()=>{
        console.log("siva")
    })

    process.nextTick(()=>{
        console.log("process.nextTick()")
    })

    Promise.resolve(console.log("promise object"))

    setTimeout(()=>{
        console.log("setTimeout")
    },1000)

    function add(){
        console.log(`function is ${a}`)
    }

    add()

    console.log("console.log of this code")


-----------------------------------*Create server*----------------------------------------

server = A server is a computer or software that listens to requests from other 
        computers (called clients) and sends back responses. 

web    = for suppose we have unique ip address. we are connect together it is nothing but web 

        NOTE:
            cilent is request to the server with the help of socket 
        NOTE:
            server sending data packets(chunk).not sending full of data 
IP     = internet protocal 

NOTE : have different types of servers.like hardware,software,app 

NOTE : we are requesting.that is http server which means application server 

STREMS = is nothing but connection in simple 
buffers = is nothing but the data is passed a chunks.....

NOTE =>=>we are understand only url(http://namaste.dev.com) but server is understanding a IP address 
        but before sending request it connect a DNS(domain name server) is nothing but connection between ip and url 
socket => it is nothing but mutlipulle cilent are send request to the server with the socket connection. if get respond then time socket is stop....
=========>by creating server using node js. in node js core module is their (http). in http have http.createServer(function(req,res){})


--------------------------------------**Database sql&Nosql*-----------------------------------------------

Database = batabase is an origanized store of data.
DBMS(Database management system) = it is a software that is intreact with end user,application and database 
                                    its self capture and analayize th data...... 

--------------------------------------------**creating database & mongoDB**------------------------------------------- 
NOTE : to ways to create mongoDB 
(1)
